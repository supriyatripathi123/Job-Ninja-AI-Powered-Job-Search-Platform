import React from 'react';

const HeroSection = () => {
  return (
    <section className="hero">
      <h2>Your Career, Supercharged by AI</h2>
      <p>One platform. Smart tools. Endless job possibilities.</p>
      <button id="startBtn">Start Now</button>
    </section>
  );
};

export default HeroSection;
